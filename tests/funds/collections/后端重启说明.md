# 后端服务重启说明

## 🐛 问题现象

```
POST http://localhost:3000/api/funds/collections/fund_basic_info/refresh 404 (Not Found)
```

## 🔍 原因

后端代码已修改，但后端服务**未重启**，新的路由和功能未生效。

---

## ✅ 解决方法

### 方法1: 重启后端服务（推荐）

1. **停止当前后端服务**
   - 找到运行后端的终端窗口
   - 按 `Ctrl + C` 停止服务

2. **重新启动后端**
   ```bash
   # 在项目根目录
   python -m app.main
   
   # 或者使用uvicorn
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

3. **验证启动成功**
   - 看到日志：`Uvicorn running on http://0.0.0.0:8000`
   - 访问：http://localhost:8000/docs

---

### 方法2: 使用热重载（如果已配置）

如果后端使用了 `--reload` 参数启动：

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

则应该**自动重载**，但有时需要手动触发：
- 保存任意Python文件即可触发重载
- 或者重启服务

---

## 🧪 验证步骤

### 1. 检查后端日志

启动后应该看到：
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [xxxxx] using StatReload
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

### 2. 访问API文档

访问 http://localhost:8000/docs

查找以下端点：
- `POST /api/funds/collections/{collection_name}/refresh`
- 应该能看到 `fund_basic_info` 作为可用的collection_name

### 3. 测试API

在浏览器或Postman中测试：

**端点**: `POST http://localhost:8000/api/funds/collections/fund_basic_info/refresh`

**Headers**:
```
Authorization: Bearer <your_token>
Content-Type: application/json
```

**预期响应**:
```json
{
  "success": true,
  "data": {
    "task_id": "xxx-xxx-xxx",
    "message": "刷新任务已创建"
  }
}
```

---

## 📋 完整重启步骤

### Windows PowerShell

1. **停止后端**
   ```powershell
   # 如果在运行中的终端，按 Ctrl+C
   # 或者找到Python进程并终止
   Get-Process python | Stop-Process
   ```

2. **启动后端**
   ```powershell
   cd f:\source_code\云子量化
   python -m app.main
   ```

### Linux/Mac Terminal

1. **停止后端**
   ```bash
   # 在运行中的终端按 Ctrl+C
   # 或者
   pkill -f "python -m app.main"
   ```

2. **启动后端**
   ```bash
   cd /path/to/云子量化
   python -m app.main
   ```

---

## 🔄 修改的文件清单

需要重启才能生效的修改：

1. **`app/services/fund_data_service.py`**
   - 新增 `save_fund_basic_info_data()`
   - 新增 `clear_fund_basic_info_data()`

2. **`app/services/fund_refresh_service.py`**
   - 新增 `_refresh_fund_basic_info()`
   - 注册 `fund_basic_info` 处理器

3. **`app/routers/funds.py`**
   - 更新清空端点支持 `fund_basic_info`

---

## ⚠️ 常见问题

### Q1: 重启后还是404？

**检查**:
1. 确认后端真的重启了（查看启动时间）
2. 确认端口是8000
3. 确认没有其他Python进程占用端口
4. 查看后端日志是否有错误

**解决**:
```bash
# 确保完全停止
pkill -9 -f "python -m app.main"

# 重新启动
python -m app.main
```

### Q2: 前端还是报错？

**刷新前端**:
1. 清除浏览器缓存：`Ctrl + Shift + R`
2. 或重启前端开发服务器

### Q3: 找不到刷新端点？

**检查导入**:
确认 `app/main.py` 已导入并注册基金路由：
```python
from app.routers import funds as funds_router
app.include_router(funds_router.router)
```

### Q4: 任务管理器错误？

**检查依赖**:
```bash
pip install motor pymongo asyncio
```

---

## ✅ 验证成功标志

重启并验证成功后，应该看到：

1. **后端日志**
   ```
   INFO: Started server process [xxxxx]
   INFO: Application startup complete.
   ```

2. **前端请求成功**
   ```
   ✅ API响应: 200 /api/funds/collections/fund_basic_info/refresh
   ```

3. **功能正常**
   - 点击"更新数据"成功
   - 进度条正常显示
   - 数据成功保存

---

## 🎯 快速命令

**一键重启**（需要先Ctrl+C停止）:
```bash
cd f:\source_code\云子量化 && python -m app.main
```

---

**重启完成后再试！** 🚀
